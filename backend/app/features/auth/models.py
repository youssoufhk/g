"""Auth models placeholder.

Real models (tenant-scoped ``app_users``, ``ops_users``, ``portal_contacts``
and the three session tables) land in the first Alembic migration of
Phase 3. The skeleton keeps this file empty so the feature folder is
self-contained per M2.
"""
